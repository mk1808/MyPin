server:
  port: 8084

spring:
  application:
    name: "synchronization"
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    password: admin
#    url: jdbc:postgresql://localhost:5432/myPin
#    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

info:
  app:
    info: "Synchronization microservice"
    
management:
  info:
    java:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"

